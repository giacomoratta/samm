const configField = require('../../../__trash__/configField.class')

describe('configField class and object', function() {

    it("should create an empty configField", function() {
        const configField1 = new configField()

        //expect(configField.isValid()).toEqual(false)
    })

})
