const SpheroidCache = require('./spheroidCache.class')

/*
 * circular FIFO cache if this.max defined
 * set by label or not
 * remove by label, by id or last one
 * get by label, by id
 * first
 * last
 *
 */

module.exports = {
  SpheroidCache
}
