process.env.ABSOLUTE_APP_PATH = __dirname
const { Config } = require('../config')
const { QueryJsonFile } = require('../query/queryJsonFile.class')

describe('query endpoints', function () {
    it('should create and handle a query file', function () {

        // delete queries file

        // new QueryJsonFile
        // check file after load

        // get invalid
        // remove invalid
        // add by label and querystring

        // get new label
        // remove label
        // get label = null

        // add by label1
        // add by label2
        // add by label3

        // foreach

        // save

        // openfile and check internal json

        // create new QueryJsonFile
        // get by labe1
        // get by label2
        // get by label3
        // foreach
        // remove label2
        // get label2 = null
    })
})
