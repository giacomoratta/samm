// if queryString: new pathbased, index foreach, add to sampleset
// if pathbased: index foreach, add to sampleset
// cache by pathBased: obj, sampleset, latest lookup
// cache: latest set, latest lookup

// cacheByPathQuery
// cacheByLatest

// getSampleSet({ sampleIndex, queryString, queryLabel, pathBasedQuery })
// getSampleLook({ sampleIndex, queryString, queryLabel, pathBasedQuery })

// getLatestSampleSet
// getLatestSampleLook
